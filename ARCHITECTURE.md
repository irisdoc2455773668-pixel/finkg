# Finkg 系统架构文档

> 版本：5.0.0 | 最后更新：2026 年 7 月

---

## 目录

1. [系统架构总览](#1-系统架构总览)
2. [后端架构](#2-后端架构)
   - [2.1 API 层](#21-api-层)
   - [2.2 NLP 引擎](#22-nlp-引擎)
   - [2.3 爬虫系统](#23-爬虫系统)
   - [2.4 市场数据集成](#24-市场数据集成)
   - [2.5 知识图谱](#25-知识图谱)
   - [2.6 报告生成器](#26-报告生成器)
3. [前端架构](#3-前端架构)
   - [3.1 组件架构](#31-组件架构)
   - [3.2 状态管理](#32-状态管理)
   - [3.3 路由设计](#33-路由设计)
   - [3.4 可视化方案](#34-可视化方案)
4. [数据管道](#4-数据管道)
5. [数据库设计](#5-数据库设计)
6. [部署架构](#6-部署架构)
7. [安全设计](#7-安全设计)

---

## 1. 系统架构总览

Finkg 采用典型的前后端分离架构，后端基于 Python FastAPI 构建，前端基于 Vue 3 + TypeScript 实现。系统整体遵循分层设计原则，各层职责清晰、解耦独立。

### 架构层次图

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                              用户层 (Clients)                                │
│                    Browser / API Client / curl / wscat                       │
└───────────────────────────────────┬──────────────────────────────────────────┘
                                    │
┌───────────────────────────────────▼──────────────────────────────────────────┐
│                          接入层 (Access Layer)                               │
│                                                                            │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                      Nginx (反向代理 / 静态文件)                       │  │
│  │                  CORS 中间件 / Rate Limiting / SSL                     │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────┬──────────────────────────────────────────┘
                                    │
┌───────────────────────────────────▼──────────────────────────────────────────┐
│                         API 层 (Application Layer)                           │
│                                                                            │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                    FastAPI Application Server                          │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────────┐  │  │
│  │  │  News    │ │ Analysis │ │  Market  │ │  Graph   │ │  Report    │  │  │
│  │  │  Routes  │ │  Routes  │ │  Routes  │ │  Routes  │ │  Routes    │  │  │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └────────────┘  │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐                              │  │
│  │  │Pipeline  │ │ Narrative│ │ Settings │                              │  │
│  │  │ Routes   │ │ Routes   │ │  Routes  │                              │  │
│  │  └──────────┘ └──────────┘ └──────────┘                              │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────┬──────────────────────────────────────────┘
                                    │
┌───────────────────────────────────▼──────────────────────────────────────────┐
│                          服务层 (Service Layer)                               │
│                                                                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌────────────────┐  │
│  │  NLP Engine  │  │   Crawler    │  │ Market Data  │  │  Report Gen    │  │
│  │              │  │              │  │              │  │                │  │
│  │ ┌─ Rule     │  │ ┌─ RSS     │  │ ┌─ Equity    │  │ ┌─ LLM Agent  │  │  │
│  │ ├─ ML       │  │ ├─ Web     │  │ ├─ FX        │  │ ├─ Template   │  │  │
│  │ └─ LLM      │  │ └─ Sched  │  │ ├─ Crypto    │  │ └─ Export     │  │  │
│  │              │  │              │  │ └─ Macro     │  │                │  │
│  │  Sentiment   │  │  RSS Parser  │  │  Integration │  │  Formatting    │  │
│  │  Entity      │  │  HTML Parse  │  │  Update Svc  │  │  Scheduling    │  │
│  │  Topic       │  │  Dedup       │  │  Calc Stats  │  │                │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  └────────────────┘  │
│                                                                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                      │
│  │ Narrative    │  │ Knowledge    │  │  LLM Service │                      │
│  │ Analysis     │  │   Graph      │  │              │                      │
│  │              │  │              │  │ ┌─ OpenAI   │                      │
│  │ Theme Track  │  │ Entity-Rel   │  │ ├─ DeepSeek │                      │
│  │ Evolution    │  │ Relationship │  │ └─ Dify     │                      │
│  │ Strength     │  │ Graph Query  │  │              │                      │
│  │ Propagation  │  │ Centrality   │  │  Proxy Spprt│                      │
│  └──────────────┘  └──────────────┘  └──────────────┘                      │
└───────────────────────────────────┬──────────────────────────────────────────┘
                                    │
┌───────────────────────────────────▼──────────────────────────────────────────┐
│                          数据层 (Data Layer)                                 │
│                                                                            │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐                │
│  │  PostgreSQL    │  │    SQLite      │  │    Redis       │                │
│  │  (生产环境)    │  │  (开发环境)     │  │  (缓存/队列)    │                │
│  │                │  │                │  │                │                │
│  │  NewsArticles  │  │  Same Schema  │  │  Cache: API    │                │
│  │  MarketIndex   │  │  Easier Debug │  │  Queue: Celery │                │
│  │  Entities      │  │  No Server    │  │  Session: 可选 │                │
│  │  Relations     │  │               │  │                │                │
│  │  Reports       │  │               │  │                │                │
│  │  Narratives    │  │               │  │                │                │
│  └────────────────┘  └────────────────┘  └────────────────┘                │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 设计原则

1. **关注点分离**：API 层、服务层、数据层职责清晰，各层通过接口交互
2. **依赖注入**：服务层通过依赖注入使用数据库会话，便于测试和替换
3. **可插拔引擎**：NLP 引擎支持规则/ML/LLM 三种模式，通过配置动态切换
4. **异步优先**：I/O 密集型操作使用 asyncio 提高吞吐量
5. **增量处理**：所有数据管道均支持增量运行，避免重复计算

---

## 2. 后端架构

### 2.1 API 层

API 层基于 FastAPI 框架构建，采用版本化路由设计（v1），为前端和外部客户端提供 RESTful 接口。

#### 路由组织

```
/api/v1/
├── news/           # 新闻 CRUD 与抓取
├── analysis/       # NLP 分析（情感/实体/主题）
├── narratives/     # 叙事追踪与时间线
├── market/         # 市场指标数据
├── graph/          # 知识图谱查询
├── reports/        # 报告生成与管理
├── pipeline/       # 数据管道控制
├── sources/        # 新闻源管理
├── settings/       # 系统设置
└── status/         # 系统健康检查
```

#### 中间件

- **CORS 中间件**：允许前端开发服务器（localhost:5173）跨域请求
- **日志中间件**：记录所有 API 请求的路径、方法、状态码和耗时
- **错误处理**：统一异常处理，返回结构化错误响应

#### 关键实现

```python
# app/main.py — FastAPI 应用入口
# - lifespan 上下文管理器：启动时创建数据库表、种子初始数据
# - 路由包含：app.include_router(api_router)
# - CORS：配置 allow_origins 允许前端跨域
```

### 2.2 NLP 引擎

NLP 引擎是 Finkg 的核心组件，采用三层架构设计，从轻量到深度逐级递进，既保证基础分析的实时性，又能通过 LLM 实现深度语义理解。

#### 三层架构

```
                      ┌─────────────────────┐
                      │    LLM 引擎          │  ← 离线/深度分析
                      │  (OpenAI / Dify)     │
                      │  深度语义理解         │
                      │  报告生成             │
                      └──────────┬──────────┘
                                 │ 降级策略
                      ┌──────────▼──────────┐
                      │    ML 引擎           │  ← 批量分析
                      │  (scikit-learn)      │
                      │  TF-IDF / LDA / NMF  │
                      │  主题模型 + 分类      │
                      └──────────┬──────────┘
                                 │ 降级策略
                      ┌──────────▼──────────┐
                      │   规则引擎           │  ← 实时分析（默认）
                      │  (jieba + 词典)      │
                      │  情感词典匹配         │
                      │  关键词提取           │
                      │  实体正则匹配         │
                      └─────────────────────┘
```

#### 规则引擎

规则引擎是默认的 NLP 处理模式，无需外部依赖即可运行，适合实时分析和离线批量处理。

**情感分析 (`services/nlp/sentiment/rule_v2.py`)：**
- 使用 jieba 进行中文分词
- 金融领域情感词典（正面词 3000+，负面词 5000+）
- 否定词反转、程度副词加权
- 中英文混合文本支持
- 输出：复合情感得分（-1.0 ~ 1.0）和分类（正面/负面/中性/混合）

**实体提取：**
- 基于 jieba 词性标注的实体候选提取
- 正则表达式匹配金融专用实体（股票代码、货币对等）
- 自定义实体词典支持

**主题建模：**
- 基于 TF-IDF 的关键词提取
- LDA 主题模型（需要 ML 引擎支持）

#### ML 引擎

当需要更精细的语义分析时，可启用 ML 引擎：

- **TF-IDF 向量化**：将文本转换为特征向量
- **LDA 主题模型**：自动发现文档集合中的隐藏主题
- **NMF 非负矩阵分解**：替代 LDA 的主题建模方法
- **情感分类器**：基于金融语料微调的分类模型
- **实体识别**：基于上下文的实体分类

#### LLM 引擎

LLM 引擎提供最深入的语义理解能力，适用于需要精确推理的场景：

- **DeepSeek / OpenAI API**：通过配置的 API 端点调用
- **Dify 平台**：支持通过 Dify 工作流进行定制化 NLP 处理
- **提示词模板**：针对不同分析任务优化的提示词
- **流式输出**：支持 SSE 流式响应，适合长文本分析
- **代理支持**：可通过 SOCKS5 代理访问受限 API

**配置方式：**

```bash
# .env 文件
NLP_ENGINE=openai    # 可选：rule / ml / dify / openai
OPENAI_API_KEY=sk-xxx
OPENAI_BASE_URL=https://api.deepseek.com/v1  # 兼容 OpenAI SDK
```

### 2.3 爬虫系统

爬虫系统负责从各大金融新闻源获取实时信息，支持 RSS 和 Web 两种抓取模式。

#### 架构设计

```
┌───────────────────┐
│  APScheduler      │  ← 定时调度（可配置间隔）
│  调度器            │
└─────────┬─────────┘
          │ 触发
┌─────────▼─────────┐
│  爬虫管理器        │  ← 管理多个爬虫实例
│  (CrawlerManager) │     状态跟踪、错误处理
└──┬──────────┬─────┘
   │          │
┌──▼──┐  ┌───▼───┐
│ RSS │  │  Web  │
│ Feed│  │ Scrape│
└──┬──┘  └───┬───┘
   │          │
   └────┬─────┘
        │
┌───────▼───────┐
│  内容处理器    │  ← 去重、清洗、标准化
│  (Processor)  │     编码处理、HTML 清洗
└───────┬───────┘
        │
┌───────▼───────┐
│  数据库写入    │  ← 批量插入、去重
└───────────────┘
```

#### RSS 抓取

- 使用 `feedparser` 解析 RSS/Atom 源
- 支持 20+ 中英文金融 RSS 源
- 自动解析标题、摘要、发布时间、来源
- 增量抓取：仅保存新文章（基于 URL 和发布时间去重）

#### Web 抓取

- 使用 `requests` + `BeautifulSoup` 抓取和解析 HTML
- 支持自定义 CSS 选择器
- 可配置编码（utf-8 / gb2312 / gbk）
- 最小正文长度过滤

#### 调度配置

- 基于 APScheduler 的定时任务
- 可配置抓取间隔（默认每 5 分钟）
- 手动触发：通过 API 端点 `/api/v1/news/fetch`
- 失败重试机制（指数退避，最多 3 次）
- 代理支持（SOCKS5）

### 2.4 市场数据集成

市场数据模块集成全球主要金融市场的实时和历史数据，提供统一的查询接口。

#### 数据覆盖

| 类别 | 包含指标 | 数量 |
|------|---------|------|
| 股票指数 | SPX、NDX、DAX、FTSE、N225、HSI、SHCOMP、A50 | 8 |
| 外汇 | EURUSD、USDJPY、GBPUSD、USDCNY、DXY | 5 |
| 商品 | 原油、天然气、黄金、白银、铜 | 5 |
| 加密货币 | BTC、ETH | 2 |
| 债券 | 美债 2Y/10Y/3M 收益率 | 3 |
| 利率 | 联邦基金利率、中国 LPR | 2 |
| 通胀 | 中美 CPI、PPI | 4 |
| 就业 | 美国失业率 | 1 |
| GDP | 中美 GDP 增速 | 2 |
| PMI | 中国制造业 PMI | 1 |
| 风险 | VIX 恐慌指数 | 1 |
| **合计** | | **34** |

#### 数据架构

- **初始化种子**：启动时自动填充默认市场数据
- **定期更新**：通过 APScheduler 定时刷新价格数据
- **手动更新**：通过 API 端点触发单指标或全量更新
- **可扩展设计**：通过 META 字典定义指标元数据，新增指标无需修改核心逻辑

#### 更新策略

- 实时数据（股票、外汇、加密货币）：每 60 秒更新
- 日频数据（商品、债券）：每日更新
- 月频数据（通胀、PMI）：每月更新
- 季频数据（GDP）：每季更新
- 数据源可切换：支持 yfinance、akshare 等

### 2.5 知识图谱

知识图谱模块将非结构化的新闻文本转化为结构化的实体-关系网络，支持叙事传播路径分析和实体影响力计算。

#### 核心能力

- **实体识别**：从新闻文本中提取人物、组织、地点、金融工具、事件等实体
- **关系提取**：基于共现分析和语义分析建立实体间关系
- **图存储**：实体作为节点，关系作为边，存储在关系数据库中
- **路径分析**：查询两个实体之间的最短路径和所有路径
- **中心性计算**：基于图论的实体影响力分析（度中心性、介数中心性）
- **交互式查询**：通过 API 支持灵活的图查询（实体名、关系类型、深度限制）

#### 存储设计

```
[实体节点]                        [关系边]
┌─────────────────┐             ┌────────────────────┐
│ id              │──────┐     │ id                 │
│ name            │      └─────│ source_entity_id   │
│ type            │            │ target_entity_id   │
│ description     │            │ relation_type      │
│ metadata (JSON) │            │ weight             │
│ created_at      │            │ source_article_id  │
│ updated_at      │            │ created_at         │
└─────────────────┘            └────────────────────┘
```

### 2.6 报告生成器

报告生成器利用 LLM 的能力，将分析结果转化为结构化的自然语言报告。

#### 报告类型

1. **每日舆情报告**：汇总当日新闻动态、情感趋势、热点叙事
2. **叙事追踪报告**：深度分析特定叙事的发展脉络和影响
3. **市场综述报告**：结合市场数据和叙事分析的综合市场观察
4. **专题研究报告**：针对特定主题的深度分析（需手动触发）

#### 生成流程

```
┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
│ 数据收集  │──►│ 模板填充  │──►│ LLM 润色 │──►│ 格式化     │
│          │   │          │   │          │   │ 输出      │
│ 新闻数据  │   │ 分析指标 │   │ 自然语言 │   │ Markdown  │
│ 市场数据  │   │ 摘要信息 │   │ 生成润色 │   │ JSON      │
│ 叙事数据  │   │ 关键事件 │   │          │   │ HTML      │
└──────────┘   └──────────┘   └──────────┘   └──────────┘
```

#### LLM 集成

- 通过 `openai_client.py` 统一管理 LLM 调用
- 支持 OpenAI 兼容 API（DeepSeek、OpenAI 等）
- 预设角色提示词（System Prompt）和任务模板
- 支持流式生成和非流式生成两种模式
- 内置 token 计数和成本估算

---

## 3. 前端架构

前端采用 Vue 3 Composition API + TypeScript 构建，以 Naive UI 为组件库，ECharts 为可视化引擎，Pinia 管理全局状态。

### 3.1 组件架构

```
App.vue (根组件)
├── 侧边导航栏 (Naive UI Menu)
├── 顶部栏 (状态、刷新、主题切换)
└── <router-view>
    ├── DashboardView.vue    ← 仪表盘：关键指标卡片 + 趋势图表
    ├── NewsView.vue         ← 新闻浏览：列表 + 筛选 + 分页
    ├── MarketView.vue       ← 市场数据：表格 + K线 + 仪表盘
    ├── NarrativeView.vue    ← 叙事追踪：时间线 + 强度曲线
    ├── GraphView.vue        ← 知识图谱：ECharts 关系图
    ├── PipelineView.vue     ← 数据管道：DAG 状态可视化
    ├── ReportView.vue       ← 报告查看：Markdown 渲染 + 导出
    └── SettingsView.vue     ← 系统设置：引擎切换 + 源管理
```

### 3.2 状态管理

使用 Pinia 管理全局状态，当前包含一个核心 Store：

**`stores/app.ts`** — 应用全局状态：
- 系统配置信息
- 当前激活的 NLP 引擎类型
- 主题设置（暗色/亮色模式）
- 全局加载状态
- API 基础 URL 配置

### 3.3 路由设计

基于 Vue Router 4 构建的客户端路由，采用扁平结构：

```typescript
const routes = [
  { path: '/',             name: 'dashboard',  component: DashboardView },
  { path: '/news',         name: 'news',       component: NewsView },
  { path: '/market',       name: 'market',     component: MarketView },
  { path: '/narratives',   name: 'narratives',  component: NarrativeView },
  { path: '/graph',        name: 'graph',      component: GraphView },
  { path: '/pipeline',     name: 'pipeline',   component: PipelineView },
  { path: '/reports',      name: 'reports',    component: ReportView },
  { path: '/settings',     name: 'settings',   component: SettingsView },
]
```

### 3.4 可视化方案

- **ECharts**：所有图表均使用 Apache ECharts 渲染
- **Naive UI Data Table**：表格数据展示（市场指标、新闻列表）
- **关系图**：知识图谱使用 ECharts Graph 系列
- **时间线**：叙事追踪使用自定义时间线组件
- **K 线图**：市场数据中的股票走势展示

---

## 4. 数据管道

Finkg 的核心数据管道（Pipeline）定义了从数据采集到可视化的完整流程。

### 管道阶段

```
阶段 1: 数据采集 (Crawl)
  ├── RSS 订阅抓取
  ├── Web 页面抓取
  └── 市场数据更新
       │
       ▼
阶段 2: 数据清洗 (Clean)
  ├── HTML 标签去除
  ├── 编码标准化
  ├── 重复检测与去重
  └── 文本长度筛选
       │
       ▼
阶段 3: NLP 分析 (Analyze)
  ├── 情感分析
  ├── 实体提取
  ├── 主题分类
  └── 叙事关联
       │
       ▼
阶段 4: 结构化存储 (Store)
  ├── 知识图谱更新
  ├── 叙事索引更新
  └── 分析结果持久化
       │
       ▼
阶段 5: 数据服务 (Serve)
  ├── API 数据提供
  └── 前端可视化渲染
```

### 执行模式

- **全量运行**：从头到尾执行所有阶段
- **增量运行**：仅处理新增数据（基于时间戳去重）
- **手动触发**：通过 API `/api/v1/pipeline/run`
- **定时调度**：通过 APScheduler 自动执行
- **单步执行**：支持单个阶段的独立执行（调试用）

---

## 5. 数据库设计

### 数据库选择

| 环境 | 数据库 | 驱动 | 说明 |
|------|--------|------|------|
| 开发 | SQLite | aiosqlite | 零配置，无需安装数据库服务 |
| 生产 | PostgreSQL 16 | asyncpg / psycopg2 | 生产级性能，支持并发写入 |
| 缓存 | Redis 7 | redis-py | 缓存、队列、session |

### 核心表结构

#### NewsArticle（新闻文章）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | 主键 |
| title | String | 新闻标题 |
| url | String UNIQUE | 原文链接（去重键） |
| summary | Text | 摘要 |
| content | Text | 正文内容 |
| source_name | String | 来源名称 |
| source_type | String | 来源类型（rss/web） |
| language | String | 语言 |
| published_at | DateTime | 发布时间 |
| crawled_at | DateTime | 抓取时间 |
| sentiment_score | Float | 情感得分 |
| sentiment_label | String | 情感标签 |

#### MarketIndex（市场指标）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | 主键 |
| symbol | String UNIQUE | 指标代码 |
| name | String | 中文名称 |
| exchange | String | 交易所/市场 |
| category | String | 类别（equity/fx/crypto/...）|
| granularity | String | 更新频率 |
| price | Float | 当前价格 |
| change | Float | 涨跌值 |
| change_pct | Float | 涨跌幅（%） |
| unit | String | 单位 |
| source_url | String | 数据来源 URL |

#### Entity（图谱实体）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | 主键 |
| name | String UNIQUE | 实体名称 |
| type | String | 实体类型（person/org/location/financial/event）|
| description | Text | 描述 |
| metadata | JSON | 元数据 |

#### Relation（实体关系）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | 主键 |
| source_entity_id | Integer FK | 源实体 |
| target_entity_id | Integer FK | 目标实体 |
| relation_type | String | 关系类型 |
| weight | Float | 关系权重 |
| source_article_id | Integer FK | 来源文章 |

#### NarrativeTheme（叙事主题）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | 主键 |
| name | String UNIQUE | 叙事名称 |
| description | Text | 描述 |
| keywords | JSON | 关键词列表 |

#### NewsSource（新闻源）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | 主键 |
| name | String | 源名称 |
| url | String | 源 URL |
| source_type | String | 类型（rss/web）|
| language | String | 语言 |
| region | String | 地区 |
| category | String | 分类 |
| enabled | Boolean | 是否启用 |

---

## 6. 部署架构

### 开发环境

```
┌─────────────────────────────────────┐
│          开发者工作站               │
│                                     │
│  ┌────────────┐  ┌────────────┐    │
│  │ Backend    │  │ Frontend   │    │
│  │ :8765      │  │ :5173      │    │
│  │ uvicorn    │  │ vite dev   │    │
│  │ --reload   │  │ HMR        │    │
│  └─────┬──────┘  └──────┬─────┘    │
│        │                │          │
│  ┌─────▼────────────────▼─────┐    │
│  │      SQLite (.db 文件)      │    │
│  └────────────────────────────┘    │
└─────────────────────────────────────┘
```

### 生产环境 (Docker Compose)

```
┌───────────────────────────────────────────────┐
│                Docker Host                      │
│                                                 │
│  ┌───────────┐   ┌───────────┐                │
│  │  Nginx    │   │  Backend  │                │
│  │  :80/443  │──►│  :8765    │                │
│  │  静态文件  │   │  uvicorn  │                │
│  └───────────┘   └─────┬─────┘                │
│                        │                       │
│  ┌───────────┐   ┌─────▼─────┐                │
│  │  Redis    │   │PostgreSQL │                │
│  │  :6379    │   │  :5432    │                │
│  └───────────┘   └───────────┘                │
└───────────────────────────────────────────────┘
```

### 扩展建议

- **高并发场景**：增加后端实例数，前端通过 Nginx 负载均衡
- **大数据量场景**：PostgreSQL 分表分库，引入 Elasticsearch 作为全文搜索
- **长时间任务**：使用 Celery + Redis 作为异步任务队列
- **监控告警**：集成 Prometheus + Grafana 监控 API 性能和系统资源

---

## 7. 安全设计

### API 安全
- CORS 白名单控制跨域访问
- API 请求参数校验（Pydantic Schema）
- SQL 注入防护（SQLAlchemy ORM 参数化查询）
- 输入长度限制和特殊字符过滤

### 数据安全
- 数据库连接字符串不硬编码（通过环境变量注入）
- 敏感信息（API Key）仅存储在环境变量中
- SQLite 数据库文件建议添加访问权限控制

### 部署安全
- Docker 容器以非 root 用户运行
- 生产环境配置 HTTPS
- 定期更新依赖包修复已知漏洞
